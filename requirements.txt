fastapi==0.111.0
uvicorn==0.29.0
asyncpg==0.29.0
python-multipart==0.0.9
# F1.2: la plantilla PU que reparte KAMPFER es .xlsx (openpyxl), pero el
# presupuesto meta REAL del ex-gerente es .xls (BIFF) y xlrd es lo unico que lo
# lee. Se queda como via de compatibilidad para los archivos que ya existen.
xlrd==2.0.1
# F2.2/F2.7: import de costos (.xlsx) y export del RO a Excel.
openpyxl==3.1.5
# Migraciones (EPIC 0.2): Alembic usa un driver SINCRONO (psycopg2); la app sigue async con asyncpg.
alembic==1.13.2
SQLAlchemy==2.0.31
psycopg2-binary==2.9.9
Pillow==10.4.0
# Sustento de valorización en PDF (ZIP con un PDF por partida). Python puro,
# solo pip, SIN librerías de sistema ni navegador headless: embebe las fotos
# desde el disco del VPS (MEDIA_DIR) con Pillow, que ya está.
fpdf2==2.8.1
